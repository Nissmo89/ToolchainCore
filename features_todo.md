# Features to Implement (Full C/C++ Toolchain)

Below is the sequenced list of features to implement. We will build them one by one, compile/build them, and remove/mark them off as they are completed.

- [ ] **Part 6: JSON Compilation Database (compile_commands.json) Generation**
  * **Goal**: Enable generating and updating a `compile_commands.json` file inside the build directory to support LSP engines (e.g., clangd). It must track the exact command, directory, and source file for each compilation step.
