# Features to Implement (Full C/C++ Toolchain)

Below is the sequenced list of features to implement. We will build them one by one, compile/build them, and remove/mark them off as they are completed.

- [ ] **Part 7: System Include & Library Path Auto-Discovery**
  * **Goal**: Implement automatic scanning and path discovery for standard system headers and SDK library paths (MSVC CRT/Windows SDK on Windows; standard GCC/Clang system directories on Linux/macOS) so compilers find standard headers without manual include paths.
